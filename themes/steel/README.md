# Steel Theme Scaffold

Scaffold for a new Ansorum theme based on the current `tanuki` example theme structure and a Steel editorial system.

This first pass provides:

- a standalone theme directory
- base, index, section, and page templates
- header and footer partials
- a dedicated Sass token/component split for the new visual language

It is intended as a clean starting point for further implementation, not a finished visual port.
